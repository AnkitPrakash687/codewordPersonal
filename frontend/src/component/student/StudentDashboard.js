import Typography from '@material-ui/core/Typography';

export default function StudentDashboard(props) {


    return(
        <Typography>
            Student Dashboard
        </Typography>
    );

}